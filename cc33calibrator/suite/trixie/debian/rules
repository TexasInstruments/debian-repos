#!/usr/bin/make -f

%:
	dh $@ --sourcedir=cc33calibrator

override_dh_auto_build:
	dh_auto_build -- CFLAGS="$(CFLAGS) -I/usr/include/libnl3/ -DCONFIG_LIBNL32" LDFLAGS="$(LDFLAGS) -L/usr/lib/" "NLVER=3"

override_dh_missing:
	dh_missing --list-missing

